<%= simple_form_for( draft, :url => finalize_production_team_assignment_url,
      :method => :post , 
      :html => {:class => "form-horizontal prevent_double_submit ajax-form" ,
        :id => "finalizer"}) do |f| %>

 
        <%= hidden_field_tag 'entity_id' , draft.id  %>
  <div class="form-actions">
    <%= f.button :submit , :class => "btn green", :value=> "Finalize Production Scheduling" %>
  </div>
<% end %>